{# templates/voyage/form.html.twig #}
{% extends 'base.html.twig' %}
{% block title %}{{ mode == 'new' ? 'Nouveau voyage' : 'Modifier ' ~ voyage.titre }}{% endblock %}

{% block body %}
<div class="page-header">
  <h1>{{ mode == 'new' ? '✈️ Nouveau <span>Voyage</span>' : '✏️ Modifier <span>' ~ voyage.titre ~ '</span>' }}</h1>
  <a href="{{ path('voyage_index') }}" class="btn btn-outline">← Retour</a>
</div>

{{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}
<div class="form-card">

  <div class="form-group">
    {{ form_label(form.titre) }}
    {{ form_widget(form.titre) }}
    {{ form_errors(form.titre) }}
  </div>

  <div class="form-row">
    <div class="form-group">
      {{ form_label(form.destination) }}
      {{ form_widget(form.destination) }}
      {{ form_errors(form.destination) }}
    </div>
    <div class="form-group">
      {{ form_label(form.paysDepart) }}
      {{ form_widget(form.paysDepart) }}
      {{ form_errors(form.paysDepart) }}
    </div>
  </div>

  <div class="form-row">
    <div class="form-group">
      {{ form_label(form.dateDebut) }}
      {{ form_widget(form.dateDebut) }}
      {{ form_errors(form.dateDebut) }}
    </div>
    <div class="form-group">
      {{ form_label(form.dateFin) }}
      {{ form_widget(form.dateFin) }}
      {{ form_errors(form.dateFin) }}
    </div>
  </div>

  <div class="form-row">
    <div class="form-group">
      {{ form_label(form.prix) }}
      {{ form_widget(form.prix) }}
      {{ form_errors(form.prix) }}
    </div>
    <div class="form-group">
      {{ form_label(form.devise) }}
      {{ form_widget(form.devise) }}
      {{ form_errors(form.devise) }}
    </div>
  </div>

  <div class="form-row">
    <div class="form-group">
      {{ form_label(form.statut) }}
      {{ form_widget(form.statut) }}
      {{ form_errors(form.statut) }}
    </div>
    <div class="form-group">
      {{ form_label(form.placesTotal) }}
      {{ form_widget(form.placesTotal) }}
      {{ form_errors(form.placesTotal) }}
    </div>
  </div>

  <div class="form-group">
    {{ form_label(form.imagePath) }}
    {{ form_widget(form.imagePath) }}
    {{ form_errors(form.imagePath) }}
    {% if voyage.imagePath %}
      <img src="{{ voyage.imagePath }}" style="margin-top:10px;max-height:100px;border-radius:8px;" alt="preview">
    {% endif %}
  </div>

  <div class="form-group form-row full">
    {{ form_label(form.description) }}
    {{ form_widget(form.description) }}
    {{ form_errors(form.description) }}
  </div>

  <div class="form-group">
    <label class="checkbox-label">
      {{ form_widget(form.populaire) }}
      {{ form_label(form.populaire) }}
    </label>
  </div>

  <div style="display:flex;gap:12px;justify-content:flex-end;margin-top:8px;">
    <a href="{{ path('voyage_index') }}" class="btn btn-outline">Annuler</a>
    <button type="submit" class="btn btn-gold">
      {{ mode == 'new' ? '✅ Créer le voyage' : '💾 Sauvegarder' }}
    </button>
  </div>

</div>
{{ form_end(form) }}
{% endblock %}
